// const orm = require("../config/orm");

// orm.selectAll();
// //"SELECT * FROM ??"
// orm.insertOne();
// //`INSERT INTO ?? (??, ??) VALUES ("??", ??)`;
// orm.updateOne();
// //`UPDATE ?? SET ?? = "??" WHERE ?? = "??"`
